import { grpc } from '@improbable-eng/grpc-web';
/**
 * @hidden
 */
export default class BaseGrpcConsumer {
    protected endpoint: string;
    constructor(endpoint: string);
    protected request<TRequest extends grpc.ProtobufMessage, TResponse extends grpc.ProtobufMessage, S extends grpc.UnaryMethodDefinition<TRequest, TResponse>>(request: TRequest, service: S): Promise<TResponse>;
}
